[project]
name = "mri-pet-synth"
version = "0.1.0"
requires-python = ">=3.9"
description = "Multi-squence MRI to multi-tracer PET synthesis"

[tool.setuptools.packages.find]
where = ["."]
include = ["mri_pet_synth*"]
